
```
graph TD
s1[按键抬起触发事件] --> s2[获取文本框的内容]

s2 --> s3[与数据进行匹配]

s3 -- 匹配成功 --> s41[获取匹配数组]
s3 -- 匹配失败 --> s42{是否存在选择框}

s41 --> s51{判断是否有对话框}
s42 --> |是|s53[删除选择框]
s42 --> |否|s54[不用操作]

s51 --> |是|s611[将匹配的数据重新写入选择框]
s51 --> |否|s612[新建选择框并且写入数据]


```
